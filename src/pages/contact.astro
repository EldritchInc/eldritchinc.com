---
import Layout from "../layouts/Layout.astro";
---

<Layout
  title="Contact"
  description="Contact Eldritch, Inc. about Azathoth, AI systems engineering, collaboration, or investment."
>
  <section class="hero">
    <div class="site-container hero-inner">
      <p class="eyebrow">Contact</p>

      <h1>
        Talk to
        Eldritch.
      </h1>

      <p class="lede">
        We’re interested in conversations about AI systems,
        open-source infrastructure, technical collaboration,
        and the future of Azathoth.
      </p>
    </div>
  </section>

  <section class="contact">
    <div class="site-container contact-grid">
      <div>
        <p class="eyebrow">Direct contact</p>

        <h2>
          Eris Discordia Montano
        </h2>

        <p class="role">
          Founder & Principal Engineer
        </p>
      </div>

      <div class="details">
        <article>
          <p class="label">Email</p>

          <a href="mailto:eris@eldritchinc.com">
            eris@eldritchinc.com
            <span aria-hidden="true">→</span>
          </a>
        </article>

        <article>
          <p class="label">GitHub</p>

          <a
            href="https://github.com/EldritchInc"
            target="_blank"
            rel="noreferrer"
          >
            github.com/EldritchInc
            <span aria-hidden="true">↗</span>
          </a>
        </article>

        <article>
          <p class="label">Location</p>

          <p class="value">
            Brooklyn, New York
          </p>
        </article>
      </div>
    </div>
  </section>

  <section class="conversation">
    <div class="site-container conversation-grid">
      <div>
        <p class="eyebrow eyebrow-light">Start a conversation</p>

        <h2>
          Building something
          difficult?
        </h2>
      </div>

      <div class="conversation-copy">
        <p>
          Eldritch is building infrastructure for increasingly complex
          AI systems and is interested in conversations with engineers,
          open-source contributors, organizations, and investors working
          on adjacent problems.
        </p>

        <a
          class="button"
          href="mailto:eris@eldritchinc.com"
        >
          Email Eris
          <span aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero {
    padding-block: clamp(6rem, 13vw, 12rem);
    border-bottom: 1px solid var(--color-border);
  }

  .eyebrow {
    margin: 0 0 1.5rem;
    color: var(--color-accent);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .eyebrow-light {
    color: #9dbbac;
  }

  .hero h1 {
    max-width: 950px;
    margin: 0;
    font-size: clamp(4rem, 9vw, 8rem);
    font-weight: 600;
    letter-spacing: -0.06em;
    line-height: 0.92;
  }

  .lede {
    max-width: 720px;
    margin: 2.5rem 0 0;
    color: var(--color-muted);
    font-size: clamp(1.1rem, 2vw, 1.35rem);
    line-height: 1.65;
  }

  .contact {
    padding-block: clamp(6rem, 12vw, 11rem);
    border-bottom: 1px solid var(--color-border);
  }

  .contact-grid,
  .conversation-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: clamp(3rem, 8vw, 9rem);
  }

  .contact h2,
  .conversation h2 {
    margin: 0;
    font-size: clamp(2.75rem, 5vw, 5rem);
    font-weight: 500;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .role {
    margin: 1rem 0 0;
    color: var(--color-muted);
    font-weight: 700;
  }

  .details {
    border-top: 1px solid var(--color-border);
  }

  .details article {
    padding-block: 2.25rem;
    border-bottom: 1px solid var(--color-border);
  }

  .label {
    margin: 0 0 0.75rem;
    color: var(--color-muted);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .details a {
    display: inline-flex;
    gap: 0.75rem;
    color: var(--color-foreground);
    font-size: clamp(1.15rem, 2vw, 1.5rem);
    font-weight: 600;
    text-decoration: none;
  }

  .details a:hover,
  .details a:focus-visible {
    color: var(--color-accent);
  }

  .value {
    margin: 0;
    font-size: clamp(1.15rem, 2vw, 1.5rem);
    font-weight: 600;
  }

  .conversation {
    background: var(--color-foreground);
    color: var(--color-surface);
  }

  .conversation-grid {
    padding-block: clamp(7rem, 14vw, 13rem);
  }

  .conversation-copy {
    max-width: 650px;
  }

  .conversation-copy p {
    margin: 0;
    color: #b8bdb9;
    font-size: 1.08rem;
    line-height: 1.75;
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.8rem;
    min-height: 3.25rem;
    margin-top: 2.5rem;
    padding: 0.75rem 1.25rem;
    border: 1px solid var(--color-surface);
    color: var(--color-surface);
    font-size: 0.9rem;
    font-weight: 700;
    text-decoration: none;
    transition:
      background-color 150ms ease,
      color 150ms ease;
  }

  .button:hover,
  .button:focus-visible {
    background: var(--color-surface);
    color: var(--color-foreground);
  }

  @media (max-width: 800px) {
    .contact-grid,
    .conversation-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 600px) {
    .hero h1 {
      font-size: clamp(3.5rem, 18vw, 5.5rem);
    }

    .details a {
      overflow-wrap: anywhere;
    }

    .button {
      justify-content: space-between;
      width: 100%;
    }
  }
</style>